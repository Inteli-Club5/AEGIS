# Local Docker-only PostgreSQL values.
# Copy to .env only for local development. Never commit real credentials.

POSTGRES_USER=aegis
POSTGRES_PASSWORD=aegis_dev
POSTGRES_DB=aegis_dev
POSTGRES_PORT=5432

