import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Protect an agent — AEGIS",
};

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
